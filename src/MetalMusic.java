public class MetalMusic extends MusicComposition {
    public MetalMusic(int lengthInSeconds, String genre, String composer, String name, int BPM) {
        super(lengthInSeconds, genre, composer, name, BPM);
        this.genre = "Metal";
    }
}
